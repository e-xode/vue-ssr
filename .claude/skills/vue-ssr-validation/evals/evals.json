{
  "skill_name": "vue-ssr-validation",
  "schema": "evals/1",
  "evals": [
    {
      "id": 1,
      "name": "trigger-tests-not-running",
      "kind": "content",
      "prompt": "Why didn't the tests run after my change?",
      "assertions": [
        {
          "text": "Explains the short-circuit table for non-code files and the dirty-list evaluation that determines when validation runs"
        }
      ]
    },
    {
      "id": 2,
      "name": "trigger-validation-pipeline",
      "kind": "content",
      "prompt": "How does the validation pipeline work?",
      "assertions": [
        {
          "text": "Describes the format/lint/build/test battery behind npm run validate, the serial lint prefix then parallel stages, and the git digest sentinel for change detection"
        }
      ]
    },
    {
      "id": 3,
      "name": "negative-api-route",
      "kind": "content",
      "prompt": "How do I add a new API route?",
      "assertions": [
        {
          "text": "Does NOT trigger the validation skill; API routing questions route to vue-ssr-architecture instead"
        }
      ]
    }
  ]
}
